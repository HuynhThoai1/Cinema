{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Cinema (Codespaces)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/out/cinema_app",
      "args": [],
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "Build Cinema (Codespaces)"
    }
  ]
}
